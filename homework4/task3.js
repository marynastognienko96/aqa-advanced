
// A muptiplication table created using for loop
 let number1 = 4;

 for (i=0; i <= 10; i++) {
     console.log(`${number1} * ${i} = ${number1 * i}`)
 }

// A muptiplication table created using while loop
let count = 0;
let number2 = 2;
while (count <= 10)
{
    console.log(`${number2} * ${count} = ${number2 * count}`)
    count++
}